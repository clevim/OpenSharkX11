# Maintainer: Clevs
# AUR: sharkctl-git — builds from GitHub main branch
pkgname=sharkctl-git
pkgver=r1.g0000000
pkgrel=1
pkgdesc="Configurador nativo para o mouse Attack Shark X11 (Electron)"
arch=('x86_64')
url="https://github.com/clevim/OpenSharkX11"
license=('MIT')
depends=('electron34' 'libusb')
makedepends=('nodejs' 'npm' 'git')
provides=('sharkctl')
conflicts=('sharkctl')
install="sharkctl.install"
source=("$pkgname::git+https://github.com/clevim/OpenSharkX11.git")
sha256sums=('SKIP')

pkgver() {
  cd "$pkgname"
  printf "r%s.g%s" "$(git rev-list --count HEAD)" "$(git rev-parse --short HEAD)"
}

prepare() {
  cd "$pkgname"
  # --ignore-scripts evita setup-electron.mjs (configura só o devenv local)
  npm install --ignore-scripts --no-audit --no-fund
}

build() {
  cd "$pkgname"
  npm run build

  npx --yes @electron/asar pack . app.asar \
    --unpack-dir "node_modules/usb/**" \
    --exclude-hidden \
    -- \
    out package.json \
    node_modules/usb \
    node_modules/node-gyp-build \
    node_modules/@electron-toolkit \
    assets
}

package() {
  cd "$pkgname"

  install -Dm755 /dev/stdin "${pkgdir}/usr/bin/sharkctl" <<'LAUNCHER'
#!/bin/sh
exec electron34 /usr/lib/sharkctl/app.asar "$@"
LAUNCHER

  install -Dm644 app.asar "${pkgdir}/usr/lib/sharkctl/app.asar"
  if [ -d app.asar.unpacked ]; then
    cp -r app.asar.unpacked "${pkgdir}/usr/lib/sharkctl/app.asar.unpacked"
  fi

  install -Dm644 assets/sharkctl.desktop "${pkgdir}/usr/share/applications/sharkctl.desktop"
  install -Dm644 assets/icon.svg "${pkgdir}/usr/share/icons/hicolor/scalable/apps/sharkctl.svg"
  for size in 16 24 32 48 64 128 256 512; do
    if [ -f "assets/icons/${size}x${size}.png" ]; then
      install -Dm644 "assets/icons/${size}x${size}.png" \
        "${pkgdir}/usr/share/icons/hicolor/${size}x${size}/apps/sharkctl.png"
    fi
  done

  install -Dm644 aur/99-attack-shark-x11.rules \
    "${pkgdir}/usr/lib/udev/rules.d/99-attack-shark-x11.rules"
}
